[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"

# Single-Page-App: alle Pfade auf die (prerenderte) index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
